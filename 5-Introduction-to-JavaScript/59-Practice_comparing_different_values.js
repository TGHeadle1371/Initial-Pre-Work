// Equal vs Strictly equal (JavaScript converts strings to nums with equality, not strict equality)
// Setup
function compareEquality(a, b) {
  if (a === b) { // Change this line
    return "Equal";
  }
  return "Not Equal";
}

// Change this value to test
compareEquality(10, "10");
