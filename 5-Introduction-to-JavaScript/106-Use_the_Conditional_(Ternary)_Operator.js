// Ternary operators can be written as a one line if else
// condition ? statement-if-true : statement-if-false;
// Write a function that checks if a and b are equal, return true or false

function checkEqual(a, b) {
  return a == b ? true : false;
}

checkEqual(1, 2);
