// Loops, (while), iterates through a loop while the condition exists
// Setup
var myArray = [];

// Only change code below this line.
// While i is less than 5, push i into myArray, increment i
var i = 0;
while(i < 5) {
  myArray.push(i);
  i++;
}
